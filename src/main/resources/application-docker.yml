spring:
  config:
    activate:
      on-profile: docker

# Lê do env SECURITY_JWT_SECRET definido no docker-compose.
# Se não existir, usa valor de fallback 'test-secret' (somente para dev/test).
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:test-secret}